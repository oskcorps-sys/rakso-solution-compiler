phase: 0
created_at: "2026-06-11T00:00:00Z"
title: "Phase 0 - Genesis Setup"
description: "Initialize RAKSO Solution Compiler under SDD+ Genesis Governance without implementation work."

success_criteria:
  - "Repository has a valid STATE_SNAPSHOT.yaml."
  - "Repository has a valid PHASE_0_SPEC.yaml."
  - "Phase 0 remains REFINED until a human-authorized transition to LOCKED."
  - "PHASE_0_CONTRACT.yaml is forbidden before LOCKED."
  - "LOCKED authorizes contract creation planning, not implementation."
  - "No implementation work is allowed before the lifecycle permits it."

scope:
  included:
    - "Initialize SDD+ required artifacts."
    - "Define project mission and boundaries."
    - "Define the REFINED to LOCKED gate for Phase 0."
  deferred:
    - "PHASE_0_CONTRACT.yaml"
    - "Implementation code"
    - "Schemas"
    - "Runtime integrations"
    - "Automation workflows"

inputs:
  source_framework: "sdd-plus"
  repository: "oskcorps-sys/rakso-solution-compiler"

outputs:
  required:
    - "STATE_SNAPSHOT.yaml"
    - "PHASE_0_SPEC.yaml"

constraints:
  - "Follow SDD+ lifecycle without shortcuts."
  - "Do not create PHASE_0_CONTRACT.yaml before LOCKED."
  - "LOCKED authorizes contract creation planning only."
  - "Do not implement before the lifecycle permits implementation."
  - "Builder cannot self-approve."

assumptions:
  - "RAKSO Solution Compiler is a project born under SDD+ Genesis Governance."
  - "Phase 0 is only for initialization and scope definition."

acceptance_tests:
  - id: "AT-001"
    description: "sdd status reports Phase 0 and REFINED."
  - id: "AT-002"
    description: "sdd validate passes for STATE_SNAPSHOT.yaml."
  - id: "AT-003"
    description: "sdd validate passes for PHASE_0_SPEC.yaml."
  - id: "AT-004"
    description: "No PHASE_0_CONTRACT.yaml exists before LOCKED."
  - id: "AT-005"
    description: "LOCKED readiness review confirms contract planning is allowed only after human authorization."
  - id: "AT-006"
    description: "No implementation files exist before the lifecycle permits implementation."

dependencies:
  - "sdd-plus installed and callable through sdd.exe"
